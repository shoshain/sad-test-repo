# Spec conformance case 001 (stub)

Define deterministic checks that `feature.spec.md` includes capabilities C* and matching EARS AC* prefixes.

## Example assertion

Every `C{n}.` capability has at least one `AC{n}.` acceptance line.
